{
  "name": "typespec-binary-serializer",
  "version": "1.0.0",
  "description": "TypeSpec-based binary command serializer/deserializer code generator",
  "main": "dist/index.js",
  "bin": {
    "tbs": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli/index.js",
    "generate": "npm run build && node dist/cli/index.js generate",
    "clean": "rm -rf dist output"
  },
  "keywords": [
    "typespec",
    "binary",
    "serialization",
    "code-generator"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
